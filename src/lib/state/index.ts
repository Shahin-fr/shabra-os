// State management exports
// Provides focused, single-responsibility classes instead of monolithic StateManager

// Re-export types for convenience
export type {
  StateNode,
  StateMetadata,
  StatePriority,
  StateStatus,
  StateSubscriber,
} from '@/types/state';
